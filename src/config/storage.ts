export interface StorageConfig {
  gapiToken: string;
  firstName: string;
  lastName: string;
}

export const STORAGE_CONFIG: StorageConfig = {
  gapiToken: "gapi_token",
  firstName: "first_name",
  lastName: "last_name",
};
